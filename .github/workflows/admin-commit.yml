# DISABLED: Este workflow causaba JSON corrupto y condiciones de carrera.
# Ahora admin.js usa la API de Git de GitHub directamente.
# Mantenemos el archivo solo como referencia.
name: Admin Auto-Commit (DISABLED)

on:
  # No se activa nunca - reemplazado por API directa en admin.js
  workflow_dispatch:

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED - admin.js now uses direct Git API"
